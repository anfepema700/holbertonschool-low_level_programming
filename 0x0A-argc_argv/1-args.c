#include "main.h"

/**
*main - print number of elements in gcc
*@argc:
*@argv:
*
*Return: 0 success
**/

int main(int argc, int argv[])
{
	if (argv != 0)
	{
		_putchar(argc);
	//	printf("Cantidad de elementos: %d\n",argc );
	}
	return (0);
}
